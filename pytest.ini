[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts = -ra
# 어떤 테스트도 이 시간을 넘기면 실패 처리(무한 대기로 CI 분 소모 방지)
timeout = 60
timeout_method = thread
filterwarnings =
    ignore::DeprecationWarning
